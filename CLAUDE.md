@AGENTS.md

# Project Context
- PRD 문서: @docs/PRD.md
- 개발 로드맵: @docs/ROADMAP.md

# 커스텀 명령어

## 글 써줘_Notion_Macro

### 모드 1 — 자유 주제: `글 써줘_Notion_Macro`

괄호 없이 입력하면 오늘 날짜 기준으로 가장 주목할 만한 매크로 이슈를 AI가 직접 판단해 주제를 선정하고 글을 작성한다.

**주제 선정 기준**: 금리, 중앙은행, 환율, 유가, 미중 무역, 글로벌 경기 중 당일 시장에서 가장 임팩트 있는 이슈 1개 선택.

### 모드 2 — 키워드 지정: `글 써줘_Notion_Macro(키워드)`

괄호 안에 키워드를 넣으면 해당 주제에 집중해서 심층 글을 작성한다.

예시:
- `글 써줘_Notion_Macro(환율)` → 원·달러 환율 심층 분석
- `글 써줘_Notion_Macro(Fed)` → 연준 정책 분석
- `글 써줘_Notion_Macro(유가)` → 유가 동향 및 매크로 영향
- `글 써줘_Notion_Macro(중국경제)` → 중국 경기 및 미중 무역 분석

키워드를 중심으로 WebSearch를 2회 이상 실행해 충분한 데이터를 수집한 후 작성한다.

---

### 공통 실행 순서
1. 모드 판단: 괄호 유무로 자유 주제 vs 키워드 지정 구분
2. WebSearch로 최신 뉴스 검색 (아래 검색 전략 적용, 3회 이상)
3. 검색 결과를 바탕으로 한국어 블로그 글 초안 작성
4. **오타 검수**: 아래 체크리스트를 항목별로 직접 소리 내어 확인하듯 검토
5. 검수 후 수정사항 반영한 최종본으로 Notion MCP(`notion-create-pages`) 업로드
6. **업로드 후 검증 (필수)**: `notion-fetch`로 방금 생성한 페이지를 다시 가져와 실제 저장된 텍스트를 확인
   - MCP JSON 레이어에서 한국어가 조용히 깨질 수 있다 (예: `잇따라`→`잃따라`, `촉발할`→`쳙발할`, `통화정책`→`통화정솵`)
   - 업로드 전 검수를 통과해도 저장 후 다를 수 있으므로 fetch 검증은 생략할 수 없다
7. **깨진 글자 발견 시**: `notion-update-page`로 해당 속성 또는 `notion-update-page`의 `update_content`로 본문 수정, 재검증

---

### 검색 전략

**핵심 원칙: 명령 당일 기준 최대 1~2일 이내 기사만 사용**
- 검색 결과를 받은 후 반드시 각 기사의 **발행일을 확인**한다
- 발행일이 명령일 기준 **2일을 초과한 기사는 내용으로 사용하지 않는다**
- 발행일을 확인할 수 없는 기사도 사용하지 않는다
- 최신 기사가 충분하지 않으면 검색 쿼리를 바꿔 재검색한다

**쿼리 구성 규칙**
- 반드시 현재 연·월·일(예: `May 6 2026`)을 쿼리에 포함
- 한국어 쿼리에는 절대 예측성 키워드(예: `전망`, `예상`, `시나리오`)를 단독으로 쓰지 않는다 — 오래된 전망 기사가 상위에 노출되기 때문. 반드시 날짜와 함께 사용한다
- 3회 검색 구성:
  - 1회: 영문 + 날짜 명시 + 신뢰 소스 (Reuters, Bloomberg, CNBC, EIA, IEA, Fed)
  - 1회: 한국어 + `YYYY년 M월 D일` 명시 (국내 금융 미디어: 한국경제, 매일경제, 연합인포맥스)
  - 1회: 공식 기관 데이터 중심 쿼리 (EIA, IEA, Fed, BOK 공식 발표)

**쿼리 예시 (금리 주제, 2026년 5월 6일 명령 기준)**
```
[영문] "Fed interest rate May 6 2026 site:cnbc.com OR site:reuters.com OR site:bloomberg.com"
[한국어] "한국은행 금리 2026년 5월 6일"
[데이터] "Bank of Korea rate decision May 2026 official statement"
```

**기사 날짜 검증 절차**
검색 결과를 받은 후 글 작성 전에 다음을 확인한다:
1. 각 링크의 URL 또는 본문에서 날짜 추출
2. 명령일 기준 2일 초과 기사는 제외 표시
3. 사용 가능한 기사가 2개 미만이면 쿼리를 수정해 재검색
4. 최종적으로 사용한 기사의 날짜를 글 작성 전 명시적으로 확인

---

### 오타 검수 체크리스트

글을 Notion에 올리기 전, 아래 항목을 **순서대로 하나씩** 확인한다. 단순히 훑어보는 것이 아니라 각 문장을 소리 내어 읽듯 검토한다.

**1단계 — 인코딩·깨진 문자 (최우선)**
- [ ] 이모지가 문장 중간에 삽입되지 않았는가 (예: `협상 🐭없이` → `협상 없이`)
- [ ] 한글 글자가 이상하게 바뀌지 않았는가 (예: `그쳣으며` → `그쳤으며`, `첡` → `척`, `겋는` → `겨냥한`)
- [ ] 깨진 문자(□, ?, �)가 없는가
- [ ] 이중 공백이 없는가

**2단계 — 맞춤법·어휘**
- [ ] 동사 어미가 자연스러운가 (예: `강하하면서` → `급감하면서`, `단창적으로` → `단독으로`)
- [ ] 조사가 올바른가 (이/가, 을/를, 은/는)
- [ ] 띄어쓰기 오류가 없는가
- [ ] 문장이 중간에 끊기지 않고 완결되는가

**3단계 — 숫자·단위·고유명사**
- [ ] 숫자 단위가 일관적인가 (달러, 배럴, %, bp)
- [ ] 고유명사 표기가 통일됐는가 (Fed / FOMC / OPEC / WTI / Brent / IEA / EIA)
- [ ] 불필요한 영문 혼용이 없는가 (필요시 한글(영문) 병기)

**4단계 — 요약(속성) 별도 검수**
- [ ] 요약 문장도 본문과 동일 기준으로 검수 (요약은 본문과 별도로 작성되므로 별도 확인 필수)
- [ ] 요약이 2~3문장 이내인가

### Notion 연결 정보
- **Database ID**: `3568ccb41fec80d982d9c022fb3635f8`
- **Data Source ID**: `3568ccb4-1fec-801f-b9c0-000b6852311f`

### 글 속성 규칙
| 속성 | 규칙 |
|------|------|
| 제목 | 핵심 내용을 담은 한국어 제목 |
| 슬러그 | 영문 소문자 + 하이픈 (예: `fed-rate-hold-may-2026`) |
| 요약 | 2~3문장 한국어 요약 |
| 발행일 | 오늘 날짜 (ISO-8601) |
| 태그 | `["금리", "중앙은행", "Fed", "ECB", "BOK", "환율", "채권", "유가", "인플레이션", "미중무역", "거시경제", "시장분석"]` 중 해당하는 것 선택 |
| 상태 | `발행` |

### 글 본문 구성
- ## 핵심 요약
- ## 배경 및 원인
- ## 시장 반응
- ## 투자 시사점

---

## 글 써줘_Notion_헤드라인

명령일 당일 발행된 매크로 관련 뉴스 헤드라인을 주제별로 수집해 Notion 헤드라인 전용 데이터베이스에 저장한다.

### 실행 순서
1. 오늘 날짜 확인 (제목에 `YYYY/MM/DD` 형식 사용)
2. 매크로 주제 5~6개 선정 — 당일 뉴스 흐름 기준으로 임팩트 있는 꼭지 우선 선택
   - 선정 풀: 금리·중앙은행, 환율, 유가·에너지, 미중무역·지정학, 글로벌 경기·기업, 채권·금융시장
3. 각 주제별 WebSearch 1회 — 총 5~6회 실행 (당일 기사만 수집)
4. 전체 25개 안팎의 헤드라인을 주제별로 적절히 분배
   - 동일 사건을 다룬 중복 기사 제거 (같은 사건이면 가장 상세한 기사 1개만 유지)
   - URL 확인 필수 — 링크가 없거나 페이지 홈으로만 연결되는 기사 제외
   - **트레이딩·차트 기사 배제**: 기술적 분석, 단기 매매 아이디어, 차트 패턴만 다루는 기사 (예: Orbex, Vladimir Ribakov, DailyForex 스타일)는 제외
   - 매크로 인사이트(정책, 데이터, 시장 구조 변화)가 있는 기사만 선택
5. 아래 레이아웃에 맞게 본문 작성
6. Notion MCP(`notion-create-pages`)로 헤드라인 DB에 업로드
7. **업로드 후 검증 (필수)**: `notion-fetch`로 페이지를 가져와 URL 링크·한국어 텍스트 깨짐 확인
8. **깨진 글자 발견 시**: `notion-update-page`로 수정 후 재검증

---

### 검색 전략

**핵심 원칙: 명령 당일 발행 기사만 사용**
- 발행일이 명령일과 다른 기사는 사용하지 않는다
- URL에서 날짜를 확인할 수 없으면 검색 스니펫의 날짜를 참조한다
- 당일 기사가 부족하면 쿼리를 바꿔 재검색한다

**쿼리 구성 규칙**
- 반드시 오늘 날짜(예: `May 6 2026`)를 쿼리에 포함
- 각 주제별 1회 검색:
  - 영문 쿼리 우선 (Reuters, Bloomberg, CNBC, FT, WSJ)
  - 한국어 꼭지는 영문 검색으로 충분하지 않을 때 추가 실행
- 각 기사마다 한국어 번역 제목을 작성한다 (영문 원문은 `//` 구분자 뒤에 보조로 포함)

**헤드라인 선별 기준**
- 시장 임팩트가 큰 기사 우선
- 동일 사건 중복 제거
- 의견·칼럼·전망 기사보다 팩트 기반 속보 우선
- **트레이딩·차트 기사 배제**: 기술적 분석, 단기 매매 아이디어, 차트 패턴만 다루는 기사 제외 (예: Orbex, Vladimir Ribakov, DailyForex 스타일)

---

### 레이아웃

```
제목(속성): YYYY/MM/DD 데일리 헤드라인

[본문 시작 — 인트로 문단 없이 바로 섹션 시작]

## 주제명 (예: 금리 · 중앙은행)

- [한국어 번역 제목](기사 URL) // 영문 원문 헤드라인 // 출처
- [한국어 번역 제목](기사 URL) // 영문 원문 헤드라인 // 출처

---
## 주제명 (예: 환율)

- [한국어 번역 제목](기사 URL) // 영문 원문 헤드라인 // 출처
  ...
```

**포맷 규칙**
- 각 bullet은 `[한국어번역](URL) // 영문원문 // 출처` 형식 (공백 포함 `//` 구분자 필수, 시간 없음)
- 한국어 번역은 뉴스 의미를 담은 자연스러운 번역 (원문 음역 금지)
- 전체 25개 안팎, 주제별 적절 분배 (임팩트 큰 꼭지에 더 많이 배정)
- 구분선(`---`)으로 주제 블록 시각적으로 분리
- 인트로 문단 없이 바로 `## 섹션`으로 시작

---

### Notion 연결 정보 (헤드라인 DB)
- **Database ID**: `5b97a10042bf4257a5168889cc2b603c`
- **Data Source ID**: `d11ee165-93df-4c66-b2f2-329a18fe43da`

### 페이지 속성
| 속성 | 규칙 |
|------|------|
| 제목 | `YY/MM/DD 데일리 헤드라인` (예: `26/05/06 데일리 헤드라인`) |
| 발행일 | 오늘 날짜 (ISO-8601) |
| 상태 | `발행` |
